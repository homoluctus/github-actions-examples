name: Python PATH

on: push

jobs:
  which:
    name: Verify PYTHON PATH
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: which python
        run: which python

      - name: echo $PATH
        run: echo $PATH
